# Singles (Step 4; XKDeco blocks sheet, transitional YAML form).
#
# One-off blocks that don't fit a family's colour × block_type cartesian — hand-listed,
# one entry each. Cartesian families (content/families/) and singles share the same product
# space and the same downstream pipeline; dense regions expand via families, sparse points
# live here. None exist yet — the regular content is fully cartesian — but the loader +
# expansion are wired so a single can be added by appending one row.
#
# Schema (per entry), when populated:
#   id, familyId, preset, colorId, shape, zh, en, category, renderType, texturePath,
#   material, [kit, modelPrefix, collision, connectsBy]
[]
